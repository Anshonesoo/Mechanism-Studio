# Mechanism Studio · 转交技术方案

> 本文档面向**接手本仓库的 AI 或工程师**。目标是让读者不读完全部源码也能安全改需求。
> 凡涉及公式、字段名、执行顺序的地方都按源码逐条核对，可直接作为修改依据。
> 最后更新：对应代码状态 commit `177a11f`（`main` 分支）；本文档自身的提交为 `4e17e14`。全文 612 行。

---

## 0. 一句话定位

浏览器里的**平面机构运动仿真 + L2D 骨骼绑图**工作台。两个顶层模式共用同一套粒子式约束求解器：

- **机构模式**：连杆 / 齿轮 / 齿条 / 滑块 / 行星轮系，可拖曳、匀速/变速驱动、施外力、记轨迹
- **L2D 模式**：骨骼（=连杆）上绑图片切片，拖骨骼摆姿势 + 整体变换 + 自动摇摆
- **积木编排**（两模式通用）：Scratch 式积木 DSL，解释执行动作序列

定位是**动画演示工具**，不做参数导出、不做有限元/接触/摩擦。

---

## 1. 运行 / 构建 / 测试 / 部署

```bash
npm install          # 依赖：react 18 / react-dom 18 / zustand 4；dev: vite 5 + @vitejs/plugin-react
npm run dev          # http://localhost:5173
npm test             # node tests/engine.test.mjs && node tests/script.test.mjs  → 47 断言
npm run build        # → dist/
npm run preview      # vite 预览
```

Windows 快捷脚本（**纯 ASCII + CRLF，改这些文件时必须保持**）：

| 文件 | 作用 |
| --- | --- |
| `start.cmd` | 定位 Node → 调 `scripts/start-dev.ps1`（隐藏后台起 vite，无控制台窗口） |
| `stop.cmd` | 调 `scripts/stop-dev.ps1`，按命令行匹配 `vite/bin/vite.js` 杀进程 |
| `serve.cmd` | 前台跑 `scripts/serve-dist.mjs`，静态托管 `dist/`（默认 4173，可用 `MECHANISM_PORT` 改） |
| `deploy.cmd` | 调 `scripts/deploy-gh-pages.ps1`：构建 + 把 `dist/` 推到 `gh-pages` 分支 |

URL 查询参数（供调试/嵌入）：

| 参数 | 作用 |
| --- | --- |
| `?mode=l2d` \| `?mode=mechanism` | 起始模式 |
| `?template=<id>` | 起始模板（需在该模式模板表内） |
| `?tour=0` | 跳过新手引导 |
| `?script=1` | 直接展开积木面板 |

**部署现状**：GitHub Pages，站点 `https://anshonesoo.github.io/Mechanism-Studio/`，仓库 `https://github.com/Anshonesoo/Mechanism-Studio`（public，默认分支 `main`）。
Pages 源 = `gh-pages` 分支（不是 GitHub Actions）。
`.github/workflows/deploy.yml` 已写好但**故意未提交**（推送凭据缺 `workflow` 权限，GitHub 拒绝含 workflow 文件的 push）；本机已把它加进 `.git/info/exclude`。若之后拿到 `workflow` 权限，在网页端新建该文件即可切换成 push 自动部署。

---

## 2. 技术栈与硬约束

- **React 18 + Vite 5，零后端，纯前端**
- 状态库：`zustand` 4（`src/store/editorStore.js` 单 store）
- 无 TypeScript、无 ESLint/Prettier 配置、无 UI 框架，样式全部手写在 `src/styles/app.css`
- **渲染与物理不经过 React**：`Viewport.jsx` 用 `requestAnimationFrame` 循环直接读写引擎世界；React 只负责面板。**改动时必须维持这个边界**，否则 60fps 会被重渲染拖垮
- `vite.config.js` 里 `base: './'`，为了部署到子路径（`用户名.github.io/仓库名/`）时资源能正确解析。**不要改成 `/`**

---

## 3. 模块地图

```
index.html                # 挂载点 #root
package.json              # type: module
vite.config.js            # base './'
start.cmd stop.cmd serve.cmd deploy.cmd
scripts/
  start-dev.ps1           # 定位 node → Start-Process -WindowStyle Hidden 起 vite（--open）
  stop-dev.ps1            # 按命令行匹配杀 vite 进程
  serve-dist.mjs          # 原生 http 静态服务器，含路径穿越防护
  deploy-gh-pages.ps1     # build → git 底层对象 → push gh-pages
src/
  main.jsx                # createRoot(...).render(<App/>)，无 StrictMode
  App.jsx                 # 栅格骨架 + 引导浮层
  engine/                 # ★ 与框架无关的纯逻辑，可在 Node 里直接 import 测试
    math.js        (90)   # 向量/几何工具
    world.js       (809)  # World：节点、零件、约束装配、step、命中测试、序列化原语
    constraints.js (212)  # 6 个约束类，全部实现 solve()
    profile.js     (91)   # ω(t) 驱动函数（匀速/正弦/匀加速/方波/表达式）
    rig.js         (48)   # 绕支点整体旋转/缩放/平移（L2D 用，纯函数）
    script.js      (357)  # 积木定义 + 解释器 VM
    templates.js   (261)  # 10 个模板（7 机构 + 3 骨架）
  render/                 # 只依赖 canvas 2D
    camera.js      (41)   # 世界↔屏幕换算、缩放、frame 自适应
    renderer.js    (592)  # drawWorld：网格/零件/关节/轨迹/外力/支点标记
    sprites.js     (66)   # 图片缓存 + 骨骼局部坐标系下的贴图变换
  store/
    editorStore.js (823)  # zustand store：所有编辑动作 + 脚本 tick 驱动
  io/
    serialize.js   (97)   # serialize/restore/toFile/fromFile
  ui/                     # React 组件（14 个）
    App 侧：App.jsx / Toolbar.jsx / LeftPanel.jsx / RightPanel.jsx / LogBar.jsx
    右栏：Inspector.jsx / RigPanel.jsx / SpritePanel.jsx / LayerPanel.jsx
    画布：Viewport.jsx (562)  ← 交互最密集
    通用：Range.jsx（滑条行）/ TourGuide.jsx / ScriptDock.jsx
  styles/app.css  (986)
tests/
  engine.test.mjs (227)   # 30 断言
  script.test.mjs (164)   # 17 断言
```

总量约 **6.6k 行**（不含 node_modules/dist）。

---

## 4. 数据流（一次帧）

```
requestAnimationFrame (Viewport.jsx 的 useEffect，deps=[])
  ├─ st = useEditor.getState()               // 读最新状态，不订阅
  ├─ world.step(dt*timeScale, advance=st.running)
  │    └─ substeps × substep(h, advance)     // 见 §6
  ├─ 若 world.diverged → 清标志 + setStatus + bump
  ├─ 若 mode==='l2d' && rig.swayAmp>0 → st.rotateRig(Δ)   // 自动摇摆
  ├─ 若 scriptRunning && running → st.tickScript(dt*timeScale)
  └─ drawWorld(ctx, world, cam, view, opts)  // 纯读，无副作用
```

React 侧只在**用户操作**或**拓扑变化**时 `set()`：`commit()` 会 `world.build()` + `syncIdCounter()` + `rev+1`。

---

## 5. 数据模型

### 5.1 World（`src/engine/world.js`）

```js
world.nodes   : Map<id, Node>
world.parts   : Map<id, Part>       // 所有零件（含不可见的约束型零件 gearPair/angleMotor）
world.sprites : Map<id, Sprite>     // 图片，独立于 parts
world.constraints : Constraint[]    // 由 build() 从 parts 推导，不持久化
world.settings    : { gravity:{x,y}, damping, iterations, substeps }
world.drag        : null | { targets:[{id,x,y,ox?,oy?}], stiffness? }
world.time, world.initial, world.diverged, world.stats
```

`settings` 默认：`gravity {0,0}`（无重力）、`damping 1.6`、`iterations 12`、`substeps 4`。UI **不暴露**这些值（设计上刻意简化）。

`stats = { nodes, parts, sprites, constraints, dof }`，其中
`dof = 2×(非固定节点数) + (非 fixedAngle 的齿轮数) − constraints.length`。
`build()` 会剔除重复/无效约束，因此该值是有意义的活动度（四杆=1、行星轮系=1、自由四杆=4）。

### 5.2 Node

```js
{ id,                 // 'n1','n2'…（makeId 前缀 n）
  type:'node',
  x, y,               // 世界坐标（y 向下，与屏幕一致）
  px, py,             // 上一子步位置（用于速度回算）
  vx, vy,             // 速度
  ax, ay,             // 本子步累积加速度（外力/重力，每子步清零）
  mass, invMass,      // fixed 时 mass=0, invMass=0
  fixed,              // 固定铰
  driven,             // 瞬时标志：本子步被驱动（电机/拖拽优先）
  grab,               // 预留（当前恒 false）
  label, zIndex(40), visible }
```

`effInvMass(n) = (n.fixed || n.driven || n.grab) ? 0 : n.invMass` —— **这是无穷质量语义的唯一入口**。

### 5.3 Part 各类型字段

所有 part 共有：`id, type, zIndex, visible, label, spriteId`（`spriteId` 实际未被使用，图片走 `world.sprites`）。

| type | 关键字段 | 说明 |
| --- | --- | --- |
| `link` | `a, b`（节点 id）, `rest`（目标长度）, `width`(0.09), `color` | 刚性杆。**L2D 的骨骼就是 link**，没有独立 bone 类型 |
| `gear` | `center`（节点 id）, `radius`, `teeth`, `angle`, `fixedAngle`, `driven`, `inertia`, `invInertia`, `internal` | `inertia = r²/2`；`fixedAngle` ⇒ `invInertia=0` |
| `guide` | `a, b` | 直线导轨，无约束，只作为滑块/齿条的参考线 |
| `slider` | `node, guide, sleeve, width(0.9), height(0.5)` | 滑块/套筒，受"点在线上"约束 |
| `rack` | `node, guide, gear, sign(1), teeth, height, offset` | `offset` = 齿轮中心到导轨的有向距离（构造时算，`guideTangent` 用它） |
| `gearPair` | `a, b`（齿轮 id）, `internal` | 不可见；产生 1 个角度约束 (+ 可能的中心距约束) |
| `motor` | `node, center, radius, profile, phi, phi0, t` | 驱动 `node` 绕 `center` 做半径 `radius` 的圆周运动 |
| `angleMotor` | `gear, profile, t` | 直接驱动齿轮转角 |
| `force` | `target`（`'p:<partId>'` 或 `'n:<nodeId>'`）, `fx, fy, enabled, exprX, exprY` | 外力；若 `exprX/exprY` 存在则按 `world.time` 求值，**优先于 fx/fy** |
| `trace` | `node, points:[{x,y}], max(1400), enabled, color` | 轨迹采样 |

**`profile`（驱动函数）**：`{ preset:'constant'|'sine'|'ramp'|'square'|'expression', omega, amplitude, frequency, phase, accel, expr }`，求值见 `profile.js` 的 `omegaAt(profile, t) → ω(t)`。

### 5.4 Sprite

```js
{ id, type:'sprite', target /*partId*/, src /*dataURL*/, name,
  anchorX(0.5), anchorY(0.5),        // 图片归一化锚点
  scale,                             // 世界单位 / 图片像素
  offsetX(0), offsetY(0),            // 相对骨骼原点，骨骼局部坐标系
  angle,                             // 额外旋转（弧度）
  rotate(true),                      // 是否跟随骨骼旋转
  opacity(1), width, height,
  baseAngle, baseX, baseY,           // ★ 绑定瞬间的骨骼姿态，用于取差值
  visible, zIndex(60) }
```

---

## 6. 约束求解器

### 6.1 求解循环（`World.substep`，顺序不可随意改）

```
① 清瞬时标志：所有 node.driven=false, node.ax=node.ay=0；所有 gear.driven=false
② 若 advance（=播放中）：
     加速度 = 重力 + applyForces()（外力/表达式力，按目标节点均摊 a=F/Σm）
     px=x, py=y;  v += a*h;  v *= exp(-damping*h);  x += v*h
   否则：只做 px=x, py=y（纯投影，位置不积分）
③ applyDrivers(h, advance)   // 电机/齿轮驱动；被 this.drag 命中的节点跳过（拖拽优先）
④ 拖拽软牵引：x += (target.x - x) * k，k = drag.stiffness ?? 0.45
⑤ 约束迭代：iterations 次 × 顺序遍历 constraints[i].solve()
⑥ 速度回算：被 drag / driven / fixed 的节点 v=0；
   其余 v = (x-px)/h，并夹到 MAX_NODE_SPEED=80
```

`step(dt, advance)`：跑 `substeps` 次 substep，`advance` 时 `time += dt`，然后 `recordTraces()`，最后 `sanitize()`（任一坐标非有限或 |x|>DIVERGE_LIMIT=1e5 → `reset()` + `diverged=true`，Viewport 会提示并 bump）。

**`advance=false` 的语义**：暂停时仍每帧调用 `step(dt,false)` 做纯投影。这样暂停状态下拖拽依然能立即求解位形，且不积分外力/重力。

### 6.2 六个约束（`src/engine/constraints.js`）

PBD 通用投影：`λ = −C / Σ wᵢ|∇ᵢC|²`，`xᵢ += wᵢ ∇ᵢC λ`。`w = effInvMass`。

**① DistanceConstraint(a, b, rest, stiffness=1)** —— 刚性杆 / 齿轮中心距 / 行星架
```
C = |b−a| − rest ;  d = b−a, len=|d|
diff = ((len−rest)/len)·stiffness
a += d·diff·(wa/(wa+wb)) ;  b −= d·diff·(wb/(wa+wb))
```

**② PointOnLineConstraint(p, a, b, stiffness=1)** —— 滑块 / 套筒 / 齿条在导轨上
```
C = (bx−ax)(py−ay) − (by−ay)(px−ax)          // cross(b−a, p−a) = 0
∇a = (by−py, px−bx)
∇b = (py−ay, ax−px)
∇p = (ay−by, bx−ax)
```

**③ GearPairConstraint(ga, gb, internal, ca, cb)** —— 齿轮副（★ 见 §7）
```
s     = internal ? −1 : +1
moving= ca && cb && ca !== cb                // 中心会动（行星轮系）
ratio = moving ? (internal ? ra − rb : ra + rb) : 0
φ     = carrierAngle()                        // 解缠后的 atan2(cb−ca)
C     = ra·θa + s·rb·θb − ratio·φ − C0
∇θa = ra ;  ∇θb = s·rb
d = cb−ca, d² = |d|²
∇ca = (−ratio·dy/d²,  ratio·dx/d²)
∇cb = ( ratio·dy/d², −ratio·dx/d²)
θi += wᵢ ∇θᵢ λ ;  ca/cb 也参与投影
```

**④ RackPinionConstraint(rackNode, guideA, guideB, gear, sign)** —— 齿条副
```
u = normalize(b−a)                            // 导轨方向
offset = dot(p0−a, u) − sign·r·(θ0)          // 构造时标定
C = dot(p−a, u) − sign·r·(θ−θ0) − offset
∇p = u ;  ∇a = −u ;  ∇θ = −sign·r
```

**⑤ GuideTangentConstraint(center, a, b, h)** —— 保持齿轮中心到导轨的有向距离
```
C = cross(u, c−a) − h        // h = rack.offset
∇c = (−u.y, u.x) ;  ∇a = (u.y, −u.x)
```

**⑥ PointConstraint(p, target, stiffness)** —— 目前仅作为工具类保留，未接入 `build()`。

### 6.3 `build()`：从 parts 推导 constraints

`World.build()` 遍历 `parts` 生成约束数组，关键规则（**改这块要特别小心**）：

1. **`link` → DistanceConstraint**。用 `addRigid()` 去重：同一对节点只保留一条；**两端都 fixed 的直接跳过**。
2. **`slider` → PointOnLine**。若 p/a/b 全为 fixed 则跳过（无效约束）。
3. **`rack` → PointOnLine + RackPinion + GuideTangent**；`GuideTangent` 只在 `center/a/b` 至少一个可动时才加。
4. **`gearPair` → GearPairConstraint(+ 中心距 DistanceConstraint)**。中心距 `rest = internal ? |ra−rb| : ra+rb`；`ca === cb`（同心的内啮合）时跳过中心距约束。
5. `guide` / `motor` / `angleMotor` / `force` / `trace` **不产生约束**。
6. 算 `stats`（含 dof）。

> ⚠️ `build()` 在每次 `commit()` 时重跑，会**重置所有约束的基准值**（`GearPairConstraint.C0`、`RackPinion.offset/theta0`）。这是有意设计：改半径、加零件后啮合关系自动重新标定，不会累积漂移。

---

## 7. ★ 齿轮副的 φ 项（最容易被改错的地方）

**固定中心距齿轮**只需 `ra·θa + s·rb·θb = const`。但**行星轮系**里行星轮中心会绕太阳轮公转，此时必须引入行星架转角 `φ`（两轮中心连线的方位角）：

```
ra·θa + s·rb·θb = ratio·φ + const
```

**推导（无滑移，外啮合）**：接触点速度在公法线方向相等 → `ra·ωa + rb·ωb = (ra+rb)·φ̇`；内啮合为 `ra·ωa − rb·ωb = −(rb−ra)·φ̇`，即代码里的 `ratio = ra − rb`（internal）与 `ra + rb`（external）。

**行星轮系正确接法**（见 `templates.js` 的 `planetary()`）：

```
太阳轮(中心 S, 半径 rs)  ── 行星轮(中心 P, 半径 rp)  ── 齿圈(中心 S, 半径 rr = rs + 2rp, fixedAngle)
两个 gearPair：
  sun–planet : external   → 2θs + θp − 3φ = C0
  planet–ring: internal   → θp − 4θr + 3φ_PS = C0'
其中 φ_PS = φ_SP + π
```

由这两式可解出解析解 `ω_carrier = ω_s · rs / (rs + rr)`，测试里已逐值校验为 `1/3`（rs=2, rp=1, rr=4）。
**注意**：不存在"太阳轮–齿圈"直接啮合；必须通过行星轮中转。

**`carrierAngle()` 必须解缠**：`atan2` 在 ±π 处跳变 2π，会让 `C` 瞬间跳 3×2π≈18.85 直接把机构炸飞。做法是用 `phiPrev` 逐次展开到最近的等价角（`while (phi − phiPrev > π) phi −= 2π`）。**任何新增的角度耦合约束都要照这个模式处理。**

---

## 8. 拖拽 / 驱动 / 外力

- **拖拽**：`world.drag = { targets:[{id,x,y,ox,oy}] }`。多目标用于"整体拖部件"（`ox/oy` 是相对光标的偏移）。软牵引系数 0.45/子步，**不是硬约束**——所以拖不坏机构，杆长永远守恒。拖拽节点在速度回算时强制 `v=0`（见 §15 第 3 条）。
- **驱动**：`motor` 每子步把 `node` 写成 `center + radius·(cos φ, sin φ)`，`φ += omegaAt(profile, t)·h`；`angleMotor` 直接加 `gear.angle`。二者都靠"把节点/齿轮标记 `driven`"实现无穷质量。
- **外力**：`force.target` 用前缀区分 `p:`（部件）/ `n:`（节点）；`targetNodes()` 解析成节点数组，加速度按质量均摊。`exprX/exprY` 存在时按 `world.time` 求值（用于悬浮模板的正弦往复力）。

---

## 9. 积木编排（Scratch 式 DSL）

`src/engine/script.js` + `src/ui/ScriptDock.jsx`。

**数据结构**：`script = { blocks: Block[] }`，`Block = { id:'blk<n>', type, params:{}, children:Block[] }`。

**类别与积木全表**（`BLOCK_SPECS`，`[container]` 表示 C 形可嵌套）：

| 类别 | type | 参数 | 语义 |
| --- | --- | --- | --- |
| event | `whenRun` | —（hat） | 装饰性，VM 直接跳过 |
| control | `wait` | `sec:number=0.5` | 暂停 N 秒 |
| control | `repeat` **[container]** | `times:number=3` | 重复 N 次 |
| control | `forever` **[container]** | — | 无限重复 |
| look | `setView` | `view:select=all\|parts\|sprites` | 切显示模式 |
| look | `showPart` | `action:select=hide\|show`, `part:part` | 显隐某部件 |
| motion | `setSpin` | `part:part`, `omega:number=2` | 设驱动转速 |
| motion | `setForce` | `part:part`, `mag:number=5`, `deg:number=0` | 施加外力 |
| rig | `rotateTo` | `deg:number=15`, `dur:number=1` | 整体旋转补间 |
| rig | `scaleTo` | `scale:number=1`, `dur:number=1` | 整体缩放补间 |
| rig | `moveTo` | `x:number=0`, `y:number=0`, `dur:number=1` | 整体平移补间 |
| rig | `setSway` | `amp:number=8`, `freq:number=0.5` | 自动摇摆 |
| rig | `setPose` | — | 复位姿势 |

**VM 执行语义**（`stepVm(vm, ctx, dt)`）：

```
vm = { frames:[{list,index,remaining?,infinite?}], wait, tween, running }
stepVm:
  1. 若 tween 活跃：t += dt; k = min(1, t/dur); apply(easeOutCubic(k))
       —— 补间函数内部自己算插值，接收的是**已缓动的进度 k**
     完成时 tween=null 并 ctx.flush()
  2. 若 wait > 0：wait -= dt；未到 0 直接 return
  3. while(guard<400):
       取栈顶 frame；index 越界 → infinite? 归零 : (remaining>1? 递减归零 : 弹栈)
       否则取 block，index++，execBlock()；返回 'pause' 就 return
```

`execBlock` 对 `repeat/forever` 压栈子 frame；对 `wait` 设 `vm.wait`；对带 `dur` 的 rig 积木设 `vm.tween` 并返回 `'pause'`。

**关键设计：补间不写 React store。** `rotateTo/scaleTo/moveTo` 的 `apply(k)` 直接调 `engine/rig.js` 的 `rotateNodes/scaleNodes/translateNodes` 改世界坐标，只在补间结束时通过 `ctx.flush()` 一次性回写 `rig.angleDeg/scale/tx/ty` 并 `bump()`。否则 60fps 的 store 写入会让整棵面板树每帧重渲染。

**`setSpin` / `setForce` 的解析逻辑**（`store.setSpinResolved/setForceResolved`，不要指望用户选具体电机）：
- `setSpin`：目标若本身是 motor/angleMotor 直接用；否则找驱动该部件的 motor（`motor.node===id || motor.center===id || angleMotor.gear===id`）；再否则：齿轮→就地建 `angleMotor`，连杆→若有固定端点则就地建 `motor`。
- `setForce`：找 `target==='p:<id>'` 的已有 force，有则改 `fx/fy` 并**清空 expr**，没有则新建。

**UI 插入规则**（`store.addBlock`）：选中块是 container → 追加到其 `children`；否则插到选中块**之后**（同一 list）；未选中 → 插到 root（`whenRun` 之后）。`whenRun` 特殊：永远 `unshift` 到最前。调序 `moveBlock(id, ±1)`，容器的子块不能越出自身 list。

**示例脚本**：`demoScript(mode)`，切换模式时若 `script.blocks` 为空会自动载入。

---

## 10. L2D 骨骼变换

L2D 模式的"骨骼"**就是 `link`**（`bone` 工具只是用 `width:0.07` 创建 link）。渲染层按 `opts.mode === 'l2d'` 走 `drawBone()`（锥形多边形 + 根部圆盘）。

**整体变换是增量式的**（`src/engine/rig.js`）：

```js
rotateNodes(world, pivot, deltaRad)   // 绕支点旋转所有节点
scaleNodes(world, pivot, factor)      // 以支点为心缩放
translateNodes(world, dx, dy)
pivotOf(world, pivotId)               // 指定节点 → 第一个 fixed 节点 → 包围盒中心
```

**为什么用增量而不是"由 rest pose 重算"**：增量可以直接和"手动拖骨骼摆姿势"叠加，不需要维护 rest pose，也不需要把鼠标坐标反变换回未变换空间。代价是浮点累积误差（可忽略）。

调用方：
- `RigPanel` 的滑条：`rotateRig((v − rig.angleDeg)·π/180)` 然后 `setRig({angleDeg:v})`
- 自动摇摆：Viewport 每帧 `rotateRig(a − swayRef.current)`，`a = amp·sin(2π·freq·world.time)`；`world.time` 回退（复位）时 `swayRef=0`

**图片跟随的运动学**（`render/sprites.js`）：
```
partFrame(part) → { pos, angle }        // link: pos=节点a, angle=atan2(b−a)
世界点 = frame.pos + offset            // offset 在骨骼局部系
旋转量 = frame.angle − sprite.baseAngle + sprite.angle   （sprite.rotate 为 false 时旋转量为 sprite.angle）
```
`baseAngle/baseX/baseY` 在 `addSprite()` 时记录，所以绑定后骨骼怎么动，图片都按**相对姿态**正确跟随。

---

## 11. 渲染契约

```js
drawWorld(ctx, world, cam, view, opts)
// view = { w, h, dpr }   （CSS 像素尺寸 + devicePixelRatio）
// opts = { mode, pivot, selection, hover, showGrid, showJoints, showTraces, showForces, visibilityMode }
```

- `mode`: `'mechanism' | 'l2d'` → 决定 link 用 `drawLink` 还是 `drawBone`
- `selection`/`hover`: `{type,id} | null`
- `visibilityMode`: `'all'` 都画；`'parts'` 隐藏 sprite；`'sprites'` 隐藏零件且不画关节
- `pivot`: l2d 时画支点十字标记
- 绘制顺序：网格 → 坐标轴 → 按 `zIndex` 升序混合绘制 parts 与 sprites → 关节 → 支点 → 橡皮筋线
- `drawSprite` 的变换链：`translate(屏幕原点) → rotate(姿态) → scale(cam.scale) → translate(offset) → scale(sprite.scale) → drawImage(−anchor·尺寸)`

`Camera`（`render/camera.js`）：`toScreen/toWorld/zoomAt/panByScreen/frame(bounds,w,h,padding=70)`。世界 y 轴**向下**，与屏幕一致。

---

## 12. Store（`src/store/editorStore.js`）

zustand 单 store，`world` 实例是**模块级单例**（不在 React 树里重建）。

**state 字段**：
```
world, rev, mode, tool, selection, hover, running, timeScale,
showGrid, snap, gridStep, visibilityMode, pairInternal, gearRadius,
pending, history, future, tourOpen, showScript,
script, scriptRunning, selectedBlockId,
rig, status
```

**actions 分组**（实测共 **74** 个）：

| 组 | action |
| --- | --- |
| 通用 | `bump` `setStatus` `pushHistory` `commit(fn,{history})` |
| 模式 | `setMode` `toggleGrid` `toggleSnap` `setVisibilityMode` |
| 选择/工具 | `setTool` `select` `setHover` `setPending` `getSelected` `deleteSelection` |
| 节点 | `updateNode` `patchNodeLive` `setNodeTrace` |
| 部件 | `updatePart` `patchLive` `commitLive` `setGearSize` `setLinkLength` `setForce` `setForceExpr` |
| 新建 | `addGear` `autoMeshGear` `addSliderOnGuide` `addRackOnGuide` `addGearPair` `addMotor` `addAngleMotor` `addForceTo` `addTraceTo` |
| 图片 | `bindSprite` `updateSprite` `removeSprite` |
| 播放 | `toggleRun` `setRunning` `stepOnce` `resetSim` `setTimeScale` `clearTraces` |
| 历史 | `undo` `redo` |
| 文件 | `exportJSON` `importJSON` |
| L2D | `setRig` `rigPivot` `rotateRig` `scaleRig` `translateRig` `resetPose` `setPivotFromSelection` |
| 引导 | `openTour` `closeTour` `disableTour` `toggleScript` |
| 积木 | `patchScript` `addBlock` `removeBlock` `moveBlock` `patchBlockParams` `setSelectedBlock` `clearScript` `loadDemoScript` `runScript` `stopScript` `tickScript` `setSpinResolved` `setForceResolved` |

**`commit(fn, {history=true})` 是所有拓扑变更的唯一入口**：`pushHistory()` → `fn(world)` → `world.build()` → `syncIdCounter(world)` → `rev+1`。
`patchLive(id, patch, kind)` 用于**不重建约束**的高频修改（滑条拖拽），只 `rev+1`。

**模块级导出**：`viewportApi = { frameAll, resetView, zoomBy, requestBind }`，由 `Viewport` 在 mount 时注册。`requestBind(partId)` 打开隐藏的 file input 做图片绑定（供 Inspector 的"绑图片"按钮、L2D 右键菜单复用）。

---

## 13. 序列化（`src/io/serialize.js`）

```js
{ format:'mechanism-studio', version:1,
  settings, time, nodes:[…], parts:[…], sprites:[…], initial }
```
- `stripRuntime()` 会剔掉 `driven` 与 profile 上的 `__fn/__src` 缓存
- `restore(world, data)` 会 `world.clear()` → 重建 → `syncIdCounter()` → `build()` → 缺 `initial` 则 `captureInitial()`
- 导入/导出走 `<a download>` + `FileReader`

---

## 14. Viewport 交互（`src/ui/Viewport.jsx`）

单 canvas + 指针事件，按 `st.tool` 分支。**工具集是模式相关的**：

| 模式 | 工具 id |
| --- | --- |
| mechanism | `select` `node` `link` `gear` `guide` `slider` `rack` `motor` `force` |
| l2d | `pose` `bone` `bind` `root` |

要点：
- 平移：中键 / Alt+拖动 / （无 pan 工具）；缩放：滚轮 `zoomAt`
- `snap` 开启时 `snapPt()` 吸附到 0.25 网格；`pickOrCreateNode()` 先 `world.snapNode(p, tol(14))`，没命中才新建
- `gear` 工具：pointerdown 建中心节点+齿轮 → pointermove 实时改半径 → pointerup 调 `autoMeshGear()`
- `pose` 与 `select` 共用分支，区别是 `pose` 不允许整部件拖动（只拖节点，才符合摆姿势直觉）
- `bind` / 右键（仅 l2d）：命中骨骼 → `select` + `viewportApi.requestBind(partId)`
- `root`：命中节点 → `select` 后 `setPivotFromSelection()`
- `endPointer` 会把被拖节点的 motor 的 `phi/phi0/radius` 重新同步（避免松手回弹）

**命中测试**：`world.hitTest(point, tol) → {type:'node'|'part', id, part} | null`；节点优先于部件，部件按 `zIndex` 取最高。

---

## 15. ★ 不变量与已知陷阱（改动前必读）

按"踩过的坑"排序，每条都对应一次真实故障：

1. **`.cmd` / `.ps1` 必须 CRLF + 纯 ASCII。** LF 行尾或中文会让 `cmd.exe` 按 GBK 解析时错位吞字符，报出 `'ode'`、`'iles'` 这类莫名错误。仓库**没有** `.gitattributes`，靠 `core.autocrlf=true` 在 clone 时还原 CRLF；直接改这些文件请用 `[System.IO.File]::WriteAllText($p, $text, (New-Object System.Text.ASCIIEncoding))`，并确保源串已是 CRLF。

2. **角度耦合必须解缠。** 任何跨 ±π 的 `atan2` 都要像 `GearPairConstraint.carrierAngle()` 那样用 `phiPrev` 展开，否则约束残差瞬间跳 2π·ratio，机构会发散。

3. **拖拽节点不能累积动量。** 速度回算时对 `dragIds` 强制 `v=0`。否则拖拽位移会变成速度注入系统，自由机构（如悬浮模板）会持续加速飞出视野——表现为"机构一拖就消失"。

4. **自研力引擎不做自平衡。** 完全自由的机构上施加**恒定**外力等于恒定加速度，必然飘走。悬浮效果必须用**正弦往复力**（`force.exprX/exprY`），位置才有界。悬浮模板 `templates.js:hoverFourbar` 就是这么写的。

5. **约束要去重/判有效。** `build()` 的 `addRigid()` 对同一对节点只留一条距离约束，两端固定则跳过；`GuideTangent` 在无可动端点时跳过。不去重会让自由度统计变负、求解病态。

6. **补间禁止写 store。** 见 §9。修改 `engine/rig.js` 的调用方时注意：批量改世界坐标 + 结束时 `flush()`。

7. **React 与引擎的边界。** RAF 循环用 `useEditor.getState()` 读状态、直接读写 `world`；面板用 `useEditor(selector)` 订阅。**不要在 RAF 里 `set()` 高频状态**。

8. **`driven` 是瞬时标志**，每子步开头清零，由 `applyDrivers()` 重新置位。别把它当成持久属性持久化（`stripRuntime()` 已剔除）。

9. **`build()` 会重置约束基准值**（`C0`、`offset/theta0`）。所以"改齿轮半径后啮合错乱"不会发生——但也意味着**不能用 `build()` 之外的路径改半径**，否则基准不同步。

10. **行星轮系用两个 gearPair**（sun–planet 外 + planet–ring 内），不存在 sun–ring 直连。见 §7。

11. **`baseAngle/baseX/baseY` 是图片跟随的关键**，在 `addSprite()` 记录。若手动构造 sprite 而漏了这三个字段，图片会以世界坐标错误旋转。

12. **`vite.config.js` 的 `base:'./'` 不要改**，否则 GitHub Pages 子路径部署白屏。

13. **隐藏后台起服务不要用 `cmd /c` 嵌套引号**。`WScript.Shell.Run` 的引号处理会让 `> log 2>&1` 的重定向把命令拆坏；当前用 PowerShell `Start-Process -WindowStyle Hidden -RedirectStandardOutput`。

14. **`reset()` 依赖 `world.initial`**（模板载入时 `captureInitial()` 生成）。手动 `new World()` 后不调 `captureInitial()` 的话，发散自恢复只能清速度不能复位。

---

## 16. 扩展配方

### 16.1 加一种积木
1. `engine/script.js` 的 `BLOCK_SPECS` 加一项：`{ cat, container?, hat?, parts:[T('文字'), N('key',默认,{min,max,step}), S('key',默认,[[值,标签]]), PART('key')] }`
2. 若需执行：在 `execBlock()` 的 `switch` 加 `case`。瞬时动作 → 调 `ctx.actions.*`；有动画 → 设 `vm.tween = { t:0, dur, apply(k) }` 并 `return 'pause'`
3. 若需要新的 store 能力，在 `ctx.actions` 里白名单暴露（`tickScript` 里定义）
4. `tests/script.test.mjs` 补断言；`palette` 会自动出现（按 `cat` 分组）

### 16.2 加一种零件类型
1. `engine/world.js` 加 `addXxx()` 工厂（字段风格照抄邻居，务必带 `id/type/zIndex/visible/label`）
2. `build()` 加分支生成约束（或明确注释"无约束"）
3. `render/renderer.js` 加 `drawXxx()`，并在 `drawWorld` 的 `switch(part.type)` 注册
4. `hitTest()` 加命中形状，`targetNodes()` 加节点解析（若要支持外力/脚本）
5. `store` 加创建 action（**必须走 `commit()`**）
6. `LayerPanel` 的 `TYPE_ICON` 加图标；`serialize.js` 无需改（parts 整体 JSON 化）

### 16.3 加一个模板
在 `engine/templates.js` 写 `function myTemplate(world, opts)`，然后
- 机构类加进 `TEMPLATES`，骨架类加进 `L2D_TEMPLATES`；`applyTemplate` 会同时在两张表里查找 id
- 结构：`world.addNode(...)` → `addLink/addGear/...` → 可选 `addMotor/addGearPair/addTrace`
- **不要**自己调 `world.build()`（由 `applyTemplate` 统一调）
- 若模板要改 `settings`（如无重力/低阻尼，参考 `hoverFourbar`），在模板函数里直接改即可：`applyTemplate` 的顺序是 `clear()` → `preset.build()` → `build()`，模板里设的 `settings` 会保留

### 16.4 改驱动函数
`engine/profile.js`：加 `preset` 到 `PROFILE_PRESETS`，在 `omegaAt()` 的 `switch` 加分支。UI 侧 `Inspector.jsx` 的 `MotorEditor` 目前只暴露"匀速/正弦"两种，要放开需同步改。

### 16.5 加测试
`tests/*.test.mjs` 是**零依赖的 Node 脚本**（自写 `check(name, ok, extra)`，末尾 `process.exit(failures?1:0)`）。`package.json` 的 `test` 脚本按顺序串跑两个文件；新增文件记得加进命令。
`engine/*` 全部无 DOM 依赖，可直接 `import` 做数值断言——这是本仓库测试成本极低的原因，**请保持 engine 层的无 DOM 纯净性**。

---

## 17. 测试现状（47 断言）

**engine.test.mjs（30）**：四杆杆长守恒/曲柄绕固定铰/轨迹记录、曲柄滑块贴轨、齿轮副传动比=r2/r1 且反向、内啮合同向、齿条 `x=rθ`、行星轮系啮合守恒 + 齿圈固定 + 行星轮自转 + **行星架转速解析解 1/3** + 中心半径、悬浮机构 20s 不飘走 + 运行中拖拽不发散、越界自动复位、暂停拖曳收敛 + 拖曳带动其它铰点。

**script.test.mjs（17）**：顺序执行、等待期间不越过、重复 N 次、嵌套重复 2×3=6、`forever` 不自己结束、停止后不再执行、旋转补间中途已启动/终点角度精确/不改变杆长/flush 回写、移动补间整体平移量精确、外力积木解析到部件、所有积木默认参数齐全。

---

## 18. 已知限制 / 未做

- **无接触/碰撞/摩擦**，纯约束运动学
- **无 TypeScript / 无 Lint 配置**，靠测试和构建兜底
- **图片绑定是刚体贴图**，不是 Live2D 那种网格变形（无网格、无权重、无插值）。要做真·网格变形需要另起一套 mesh/deformer 系统
- **撤销/重做是整世界快照**（`serialize(world)` 存历史，上限 80 步），大机构会吃内存
- **74 个 store action 中有 14 个当前 UI 未接入**（保留能力，删除前先确认无外部调用）：
  `toggleSnap`（吸附恒为开——简化 UI 时移除了网格吸附按钮，`st.snap` 始终 true）、`setPairInternal`、`setGearRadius`、`getSelected`、`setRunning`、`stepOnce`、`setPendingLive`、`commitLive`、`setLinkLength`、`setForceExpr`、`addGearPair`（现由 `autoMeshGear` 自动建）、`setMotorSpin`、`addTraceTo`（现由节点勾选框走 `setNodeTrace`）、`duplicatePoseSnapshot`。
  注：`patchScript` / `pushHistory` / `commit` / `setSpinResolved` / `setForceResolved` 虽不被 UI 直接调用，但是 store 内部互调的必需动作。
- **`.github/workflows/deploy.yml` 未提交**，自动部署未启用（见 §1）
- 中键平移依赖 `pointerdown` 的 `button===1`，触屏设备上只能靠 Alt+拖动

---

## 19. 术语表

| 中文 | 代码 | 说明 |
| --- | --- | --- |
| 铰点 / 关节 | node | 粒子，自由度 2 |
| 连杆 / 骨架 | link | 两点距离约束 |
| 机架 | 两端 fixed 的 link | 无约束（被 `addRigid` 跳过） |
| 导轨 | guide | 直线参考，无约束 |
| 滑块 / 套筒 | slider | 点在线上 |
| 齿条 | rack | 点在线 + 齿条副 + 相切 |
| 齿轮副 | gearPair | 角度耦合 +（可能）中心距 |
| 内齿圈 | gear(internal:true) | 齿向内，`s=−1` |
| 行星架 | 连接太阳轮中心与行星轮中心的 link | φ 的来源 |
| 转动驱动 | motor | 驱动节点绕支点圆周运动 |
| 齿轮驱动 | angleMotor | 直接驱动齿轮转角 |
| 外力 | force | 可常数或 `expr(t)` |
| 轨迹 | trace | 按帧采样节点位置 |
| 自由度 | dof | `2·自由节点 + 自由齿轮角 − 约束数` |
| 积木 | block | 脚本节点 |
| 补间 | tween | 带 `dur` 的动画积木 |
| 整体变换 | rig transform | 绕支点增量旋转/缩放/平移 |

---

## 20. 接手后的第一条建议

```bash
npm install && npm test && npm run build     # 确认基线：47 PASS + 构建通过
npm run dev                                  # 打开 ?script=1 手动玩一遍两个模式
```

改任何 `engine/` 代码之前先跑 `npm test`，改完再跑。这套测试是纯数值断言、毫秒级、无浏览器依赖，是改动安全网。**改 `constraints.js` 或 `build()` 之后务必确认行星轮系那条 `i=0.3333` 仍然通过**——它是对整个约束组最敏感的回归指标。
